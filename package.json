{
  "name": "chromemessenger",
  "version": "1.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "typesVersions": {
    "*": {
      "*": ["dist/index.d.ts"],
      "global": ["dist/chrome-global.d.ts"]
    }
  },
  "description": "messaging framework to all natural method calling between all components of a chrome extension",
  "scripts": {
    "build": "tsc && cp src/chrome-global.d.ts dist/",
    "test": "echo \\\"Error: no test specified\\\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "ChromeMessenger"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "chrome",
    "extensions",
    "messaging",
    "async"
  ],
  "author": "Peter Maude",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.14.10"
  },
  "dependencies": {
    "typescript": "^5.5.3"
  }
}
